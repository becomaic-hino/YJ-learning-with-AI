# CHANGELOG

이 저장소의 갱신 이력. 데이터가 추가·수정될 때마다 여기에 한 줄씩 쌓인다.

## 커밋 컨벤션

| 접두어 | 용도 | 예 |
|---|---|---|
| `log:` | 새 질문·정정·재적용 기록 추가 | `log: A03 reapplied (mock-task-a)` |
| `fix:` | 기존 기록의 오류 수정 (수정 사유를 본문에 명시) | `fix: Q3 date 08-03 → 08-04 (file metadata)` |
| `docs:` | 문서·페이지 개선 (데이터 불변) | `docs: index chart tooltip` |

데이터 행을 삭제하는 커밋은 만들지 않는다. 틀린 기록도 `fix:`로 정정 이력을 남기며 고친다 — 이 저장소의 주제가 바로 그것이기 때문이다.

## 이력

### 2026-08-17 — `log:` 전형 분석·소재 설계 세션

v3 발행분 이후 첫 데이터 추가. **기록 발생일과 작성일이 같은 첫 구간이다** — 사후 재구성이 아니라 세션 종료 직후 작성했다.

⚠️ **커밋 이력으로 증명되는지는 push 시점에 달렸다.** 이 항목을 쓰는 시점에 저장소는 아직 GitHub에 push되지 않았으므로, 지금은 "당일 작성"까지만 사실이고 "당일 커밋"은 아니다. push 이후의 기록부터 커밋 시각이 증거가 된다.

- questions.csv **47행 → 53행** (L3 2건 + directive 4건, source `cowork-session`)
- corrections.csv **19건 → 26건** — B04·B05·B06(B형) / C07·C08(C형) / A05·A06(A형)
- 집계 구간을 셋으로 분리: 통신 개념 학습 / 반도체 개념 학습 / **전형 분석·소재 설계**. 8/17 기록을 도메인 비교표(learning-log 2장)에 섞지 않았다 — 섞으면 L1→L3 학습 소요 측정이 오염된다
- **learning-log 5-3 신설 — AI 실패 패턴의 재발(B03 → B04).** 같은 실패("닫을 수 있는 문제를 열어두기")가 12일 간격으로 반복됐고, B03의 재적용이 `none`이었던 것이 원인이다. `none`을 지우지 않고 남겨둔 판단이 재발 식별을 가능하게 했다
- learning-log 5-2에 A05 패턴 추가 — 분석 범위를 문제 정의로 착각
- 3차 소스(커뮤니티 속보) 의존 항목을 8/14 현장 녹취 전문으로 대체. 단 녹취는 STT 산출물이라 **1차로 등급을 올리지 않았다** (실제로 오기 1건 발견 → B04)
- 미확인(none) 8건 → 12건. **방안 확정을 재적용으로 세지 않았다.** 8/20 이후 `fix:` 커밋으로 채운다

### 2026-08-13 — v3 최초 발행
- 통신(2026-04) + 반도체(2026-08) 2개 도메인 통합본 발행
- questions.csv 47행 / corrections.csv 19행
- corrections.csv에 `reapplication_type` 컬럼 신설 (self / self-partial / output / none — 정의: docs/methodology.md 3장)
- index.html 발행 — 모든 수치는 CSV에서 로드 시점 계산
- 편집 규칙 적용: 회사명 'A사' 치환 + 생활 정보 3곳 대괄호 마스킹 (docs/methodology.md 6장)

## 예정 (기록되면 위로 올라간다)

- [ ] `fix:` **B06 재적용** — 자산 인벤토리에 「규제가 계약을 바꾼다」·「미중 통상환경 정리」 등재 (재발 방지의 유일한 수단)
- [ ] `log:` **A06 재적용** — 리포트 v2 갱신: CIT 5/7 판결, 122조 7/24 만료, Section 301 대체, 금속 232조 층위, 나스닥 조달액 265억 달러
- [ ] `fix:` **C08·B05 재적용** — 분석노트 9-1 소재 지위 격하 / 9-2 이지선다 표를 중첩 구조로 교체 (8/20 JD 확인 시 통합)
- [ ] `log:` **A05 재적용 확인** — 지원서 축② 초안 첫 문장이 문제 진술인지 범위 진술인지 점검 → self-partial → self 승격 여부 결정
- [ ] `log:` **B04 재적용 확인** — 소스 충돌 시 등급 비교를 먼저 하는지, 다음 세션에서 관찰
- [ ] 232조 2단계 발동 여부 확인 (9~10월 발효 전망) → 리포트 미결 쟁점 #1 종결
- [ ] `docs:` index.html 히어로 스탯의 "이 중 2건은 1차 자료로 검증"이 **하드코딩**이다. B형이 늘어도 자동 갱신되지 않으므로, 1차 자료 근거 여부를 corrections.csv 컬럼으로 승격할지 결정 필요 (현재 값 2는 정확)
- [ ] 모의과제 A 실전 풀이 → A01~A04 재적용 측정
- [ ] 신규 영역(LTA 세부·낸드/eSSD·base die 경제학·고객사 구매 행태) 진입 시 L1→L3 소요 기록
- [ ] 8/20 채용 공고·JD 확인 → 문항·글자 수·영마 JD·근무지 확정
