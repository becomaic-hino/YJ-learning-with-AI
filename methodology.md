---
title: 방법론 — 분류 기준·날짜 신뢰도·편집 규칙
subject: 이 저장소의 모든 데이터에 적용된 규칙의 전문
author: 최유진
created: 2026-08-13
status: v1
---

# 방법론

이 문서는 저장소의 데이터가 **어떤 규칙으로 만들어졌는지** 전부 공개한다. 규칙을 공개하는 이유는 하나다 — 제3자가 같은 기준으로 재분류·재검증할 수 있어야 이 로그가 검증 자료로 성립하기 때문이다.

---

## 1. 질문 층위 분류

### 1-1. 앵커 (기준 예시)

분류 기준을 먼저 정하고, 그 기준으로 사후 분류했다.

| 층위 | 정의 | 앵커 |
|---|---|---|
| **L1** | 용어의 뜻을 묻는 정의 질문 | "믹스가 뭐야?" (명칭) |
| **L2** | 구조·인과를 묻는 질문 | "왜 D램은 3나노로 못 가?" (인과) |
| **L3** | 변수 상충·전략 판단·방법론 설계 | "HBM 비중이 늘어도 블렌디드 ASP가 떨어지는 조건은?" (변수 상충) |
| directive | 작업 지시 — 질문 아님 | "백서 pdf로 만들어줘" |

### 1-2. 경계 하향 원칙

**경계 사례는 무조건 낮은 등급으로 분류한다.** 후하게 매긴 로그는 한 번의 반문에 무너지고, 보수적으로 매긴 로그는 공격할수록 단단해진다. 적용 사례: 1x/1y/1z·고종횡비 질문은 L2와 L3의 경계에 있으나(변수 여러 개를 동시에 물음) 상충 판단이 아니라 인과 사슬 질문이므로 **L2로 유지**했다.

### 1-3. 특수 케이스

반도체 L3 중 1건(2026-08-13)은 본인의 질문이 아니라 **AI가 낸 확인문제에 대한 본인의 답변**이다. 판단 행위이므로 L3로 분류하되, 이 사실을 learning-log.md 4장에 명시했다.

---

## 2. 정정 유형 (A/B/C)

| 유형 | 정의 | 검증력의 근거 |
|---|---|---|
| **A형** | 본인 오개념 → 교정 | 틀린 궤적이 남은 기록은 조작 유인이 없다. 정답만 나열된 로그는 누구나 만들 수 있다 |
| **B형** | AI 오류를 본인이 적발 | 유일하게 외부 검증이 개입하는 지표. 근거가 공개 원문(SEC·연방관보)이면 누구나 재확인 가능 |
| **C형** | 작업 방식 교정 | AI와의 협업 프로토콜이 어떻게 수정·정착됐는지의 기록 |

### 2-1. 정정의 발생 경로는 2종이다

정정은 ① 질문에 대한 답변 과정에서 발생하거나(→ questions.csv의 `correction_id`로 조인됨) ② **질문 없이 AI 산출물을 검토하는 과정에서** 발생한다(→ 대응하는 질문 행이 없음: B01, B02, C01, C03, C05). 조인이 비어 있는 정정은 누락이 아니라 후자다.

---

## 3. 재적용 분류 (reapplication_type)

교정받은 것을 나중에 실제로 쓴 기록만이 학습의 물증이다. 단, "재적용"이라는 말이 두 가지로 섞이면 집계가 오염되므로 범주를 분리한다.

| 값 | 정의 | 해당 |
|---|---|---|
| `self` | **본인의 사고·행동**에서 교정 내용이 재적용됨 | T-A01, T-A02, C02, C04 (4건) |
| `self-partial` | 본인 재적용이 확인되나 부분적 | A01 (1건) |
| `output` | 교정이 **문서·산출물에 반영**됨 (본인 사고의 재적용과는 구분) | T-B01, T-C02, B01, B02, C01, C06 (6건) |
| `none` | 재적용 기록 없음 — 빈 칸은 빈 칸으로 | 8건 |

`none` 8건은 그대로 둔다. 채워지는 순간마다 커밋으로 기록한다 (CHANGELOG.md의 커밋 컨벤션 참조).

---

## 4. 날짜 신뢰도 (3등급)

| 등급 | 정의 |
|---|---|
| **확정** | 파일 메타데이터(생성·수정 시각) 또는 본인 실시간 기록 있음 |
| **추정** | 앞뒤 확정 앵커 사이에서 역산, ±범위 명시 |
| **불명** | 근거 없음 — 날짜를 만들어 넣지 않는다 |

### 4-1. T0 근거 우선순위

**T0(학습 시작점)는 파일 시스템 메타데이터를 1순위 근거로 하고, AI 대화 이력의 날짜는 보조 근거로만 쓴다.** AI 대화 기록의 자체 타임라인 재구성은 신뢰 불가 — 이 원칙은 이 아카이브 안에서 실제로 검증됐다 (AI가 "8/3 스레드 개설"로 회신한 것을 스킬 파일 생성 시각 8/4 15:19로 반박, learning-log.md 7-1).

### 4-2. 측정 프로토콜 (2026-08-13 가동)

새 영역에 진입하는 질문마다 날짜 타임스탬프를 남긴다 (`[날짜 · Q번호 — 카테고리]` 포맷). 이후 축적되는 기록은 전부 '확정' 등급으로 쌓인다.

---

## 5. 자료 소스 등급

| 등급 | 정의 | 예 |
|---|---|---|
| 1차 | 법정 공시·법령 원문·회사 공식 발표 | SEC 424B4, Proclamation 11002(연방관보), 실적 컨퍼런스콜 전문 |
| 2차 | 가공·정리된 자료 | 자체 제작 백서, 언론 보도 |
| 3차 | 커뮤니티·비공식 전언 | 채용설명회 현장 속보 (작성자 1회 번복 이력 있음) |

**3차 소스는 1차 소스와 같은 무게로 다루지 않는다.** 3차 소스에 기반한 정보에는 "재확인 필요"를 명시하며, 이 표기는 발행 과정에서 삭제하지 않는다.

---

## 6. 편집(마스킹) 규칙 — 전문

원문 훼손을 최소화하기 위해 편집은 두 종류만 허용했다.

**① 회사명 치환.** 타사 **지원 이력** 맥락의 회사명은 'A사'로 치환했다 (관련 스킬명·데이터 소스명도 `a-company-*`로 함께 치환). 단, **산업 분석 대상**으로서의 기업명(경쟁사·고객사·타사 사업부 비교 등)은 그대로 두었다 — 산업 학습 기록에서 기업명을 지우면 기록 자체가 훼손되기 때문이다. 치환은 익명화가 아니라 비명시화이며, 문맥상 추정 가능성이 남는 것을 인지하고 선택했다.

**② 생활 정보 마스킹.** 질문 원문 중 생활 정보 3곳을 대괄호로 치환했다 (예: "[자리 비운 동안]", "[오프라인이라서]"). 대괄호는 치환 사실 자체를 드러내기 위한 표기다.

이 외의 편집은 없다. 반말·이모티콘·오타·구어체·틀린 추론 전부 원문 그대로다. 제3자 개인이 식별될 수 있는 표현 1곳은 역할 일반화로 익명 처리했다(corrections.csv C05).

---

## 7. 발행 원칙

- 기록에 있는 것만 수록한다. 추정은 추정이라 쓴다. 빈 칸은 빈 칸으로 남긴다.
- 문서는 .md, 데이터는 .csv만 쓴다 (기계 판독·버전관리·재추출 가능성).
- 요약 페이지(index.html)의 모든 수치는 CSV에서 로드 시점에 계산된다. 본문과 데이터가 어긋날 수 없는 구조를 채택했다.
- 커밋 이력은 발행일 이후만 증명한다. 이 한계는 숨기지 않고 첫 화면에 명시한다.
